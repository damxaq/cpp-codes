#define q l.push_back(i
#define s std::vector<
#define c d.size();
#define a ) || (d[i][0]==
#define b && d[i+1][0]==
s std::string> f, ReduceDirections(auto d) {

    s int> l;
    int k=1,i;
    while (k){
        k=c
        for(i=0; i<k-1; i++)
            if ((0 a 78 b 83 a 87 b 69 a 83 b 78 a 69 b 87)){
                q );
                q +1);
                i++;
            }
            while (!l.empty()){
                d.erase(d.begin()+l.back());
                l.pop_back();
            }
        k-=c
    }
    return d;
}

